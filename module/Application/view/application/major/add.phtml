<?php
    /**
     * I added this partial view to make it easy to render forms.
     * Just add a 'form' element to the viewModel in your action. (see MajorController::addAction())
     */
?>
<?= $this->partial('layout/partial/form', array('form' => $form)); ?>